class schedule {
    constructor(id, order_cd,examination_date,queue) {
        this.id = id;
       this.order_cd = order_cd;
        this.examination_date = examination_date;
        this.queue = queue;
        
}
}

export default schedule;