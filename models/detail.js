class detail {
    constructor(id, order_cd,order_desc,queue) {
        this.id = id;
       this.order_cd = order_cd;
        this.order_desc = order_desc;
        this.queue = queue;
        
}
}

export default detail;